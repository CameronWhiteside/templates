{
	"name": "pay-per-crawl-template",
	"version": "1.0.0",
	"description": "Cloudflare Worker template for pay-per-crawl pricing on AI crawler traffic",
	"private": true,
	"scripts": {
		"clean": "rm -rf dist",
		"build:config": "tsx scripts/build-config.ts",
		"dev": "npm run clean && npm run build:config && wrangler dev",
		"deploy": "npm run clean && npm run build:config && wrangler deploy",
		"tail": "wrangler tail",
		"types": "wrangler types"
	},
	"keywords": [
		"cloudflare",
		"workers",
		"ai",
		"crawlers",
		"pay-per-crawl",
		"bot-management"
	],
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20250124.0",
		"tsx": "^4.7.0",
		"typescript": "^5.7.3",
		"wrangler": "^3.105.0"
	}
}
